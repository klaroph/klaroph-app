export const metadata = {
  title: 'Create new password — KlaroPH',
  description: 'Set a new password for your KlaroPH account.',
}

export default function ResetPasswordLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
